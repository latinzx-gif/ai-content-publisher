import { cn } from "@/lib/utils"

export const invInputClass = cn(
  "h-9 w-full rounded-lg border border-input bg-transparent px-3 text-sm outline-none transition-colors focus-visible:border-ring focus-visible:ring-2 focus-visible:ring-ring/50"
)

export const invLabelClass = "text-sm font-medium text-foreground"
